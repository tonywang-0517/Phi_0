# @package _global_
# Smoke config for SIMPLE G1 pipeline (short run / import check).

defaults:
  - train_simple_g1_act
  - _self_

output_dir: ./experiments/simple_g1_smoke
checkpoint_name: simple_g1_smoke
max_steps: 50
save_every_steps: 25
batch_size: 2
num_workers: 0
