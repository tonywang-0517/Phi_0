# @package _global_
# Ablation 1: restore VGGT multi-frame history (Cosmos stays single-frame).

defaults:
  - train_ablation_base
  - _self_

output_dir: ./experiments/ablation1_vggt_full_400step
checkpoint_name: ablation1_vggt_full_400step

model:
  vggt_use_full_video: true
